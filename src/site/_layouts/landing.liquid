{% if site.include_open_html %}
<!DOCTYPE html>
<html lang="en" class="no-js" itemscope itemtype="http://schema.org/Article">
  <head>
{% endif %}
		{% include head.liquid %}
	</head>

	<body class="{{ page.class }}">
    {% injectdata project _project.yaml %}
    {% injectdata content _book.yaml %}

		{% include header.liquid %}

    <div class="page-header">
      <div class="container">

        {% include modules/breadcrumbs.liquid %}

        <h3 class="xxlarge">{{ page.title }}</h3>
        <p class="page-header__excerpt g-wide--push-1 g-wide--pull-1">{{page.introduction}}</p>
      </div>
    </div>

		{{ content }}

		{% include footer.liquid %}

		{% include scripts.liquid %}
	</body>
</html>
