# {# wf_auto_generated #}
# ATTENTION - This file is auto-generated, changes must be made to the template!
toc:
- title: "{{{title}}}"
  section:
  - title: "All Articles"
    path: /web/{{section}}/{{year}}/
{{#each months}}
  - title: "{{{title}}}"
    section:
    {{#each articles}}
    - title: "{{{title}}}"
      path: {{url}}
    {{/each}}
{{/each}}
