    {% include head.liquid %}
  </head>

  <body {% if page.rtl == true %}dir="rtl" {% endif %}class="page--{{ page.class }}" itemscope itemtype="http://schema.org/Article">
    {% injectdata content _betterbook.yaml %}

    {% include header.liquid %}

    <header class="spotlight-header spotlight-header--detail spotlight-header--portrait clear">
      <div class="spotlight-header__container container">
        {% include modules/breadcrumbs.liquid %}
        <div class="spotlight-header__copy g--half">
          <h1 class="xxlarge">{{ page.title }}</h1>
          <p>{{ page.introduction }}</p>
        </div>
        <div class="spotlight-header__media g--half g--last">
          <img src="{{site.baseurl}}/fundamentals/showcase/spotlight/images/{{ page.id }}/screenshot.jpg" class="spotlight-header__image">
        </div>
      </div>
    </header>

    <div class="spotlight-wrapper">
      <div class="container clear">
        <div class="content g--half g--last">
          <h2>Our thoughts</h2>
          {{ content }}
        </div>
      </div>
    </div>

    <div class="container">
      <div class="related-items-group">
        {% include modules/more_spotlights.liquid %}
        {% include modules/related_guides.liquid inline=true list=page.related %}
      </div>

    </div>

    {% include footer.liquid %}
