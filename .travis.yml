language: node_js
matrix:
  include:
    - node_js: "4.6.0"
sudo: required
dist: trusty
cache:
   directories:
     - node_modules
install:
  - npm install
script:
  - npm run clean
  - npm run build
  - npm run test
